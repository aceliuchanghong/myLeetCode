# 返回将一维数组向右旋转k个位置的结果。比如，一维数组[1,2,3,4,5]，k=2时，返回结果是[{]4,5,1,2,3]。要求常数级空间复杂度，允许修改原有数组

import generateArray as ga

dataset = ga.newArray.generateit(8)
k = 6


def solution(data=dataset, target=k):
    return


print(dataset)
print(solution())
